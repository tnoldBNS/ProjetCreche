<h1>Bonjour!</h1>

<p>Pour reset votre mot de passe, veuillez suivre le lien suivant !!</p>

<a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>

<p>Le lien va expirer dans {{ tokenLifetime|date('g') }} heure(s).</p>

<p>A bientot !</p>
<a href="{{ path('home') }}">Accueil</a>
